import * as React from 'react'

interface ITProps {}
interface ITState {}

export default class Api extends React.Component<ITProps, ITState> {
  public render(){
    return (
      <div style={{clear: 'both'}}>Api</div>
    )
  }
}