import * as React from 'react'

interface ITProps {}
interface ITState {}

export default class Tools extends React.Component<ITProps, ITState> {
  public render(){
    return (
      <div style={{clear: 'both'}}>Tools</div>
    )
  }
}