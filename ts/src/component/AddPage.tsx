import * as React from 'react'

interface ITProps {}
interface ITState {}

export default class AddPage extends React.Component<ITProps, ITState> {
  public render(){
    return (
      <div style={{clear: 'both'}}>AddPage</div>
    )
  }
}