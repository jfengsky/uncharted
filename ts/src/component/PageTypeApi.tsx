import * as React from 'react'

interface ITProps {}
interface ITState {}

export default class PageTypeApi extends React.Component<ITProps, ITState> {
  public render(): JSX.Element{
    return (
      <div style={{clear: 'both'}}>PageTypeApi</div>
    )
  }
}